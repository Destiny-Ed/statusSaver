<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.status.statussaverpro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true">

        <activity
            android:exported="true"
            android:name="com.status.statussaverpro.showImageAndVideo.ImageSavedFullScreen"
            android:configChanges="screenSize|orientation|uiMode" />
        <activity
            android:exported="true"
            android:name="com.status.statussaverpro.showImageAndVideo.videoSavedFullScreen"
            android:configChanges="screenSize|orientation|uiMode" />

        <activity
            android:exported="true"
            android:name="com.status.statussaverpro.splashScreen_and_Settings_utils.Splash"
            android:configChanges="screenSize|orientation|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:exported="true"
            android:name="com.status.statussaverpro.showImageAndVideo.videoFullScreen"
            android:configChanges="screenSize|orientation|uiMode" />
        <activity
            android:exported="true"
            android:name="com.status.statussaverpro.showImageAndVideo.ImageFullScreen"
            android:configChanges="screenSize|orientation|uiMode" />
        <activity
            android:name="com.status.statussaverpro.MainActivity"
            android:configChanges="screenSize|orientation|uiMode"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/App_Id" />
    </application>

</manifest>